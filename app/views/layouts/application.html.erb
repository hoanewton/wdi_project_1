<!DOCTYPE html>
<html>
<head>
  <title>LearningEnglish</title>
   <!-- stylesheet_link_tag    'application', media: 'all'%> -->
   <!-- javascript_include_tag 'application', 'data-turbolinks-track' => true %> -->
  <%= csrf_meta_tags %>
</head>
<body ng-app="AngularRails">
  <h1 class='top'>Learning English</h1>
  <div class="head">
    <!-- Add messages onto loggin/logout/login failure -->
    <% flash.each do |key, value| %>
      <div class="flash <%= key %>"><%= value %></div>
    <% end %>
    
  	<h3>
    <% if current_user %>
     Welcome back, <%= current_user.first_name %>!<br>
      <%= link_to "Home", root_path %> |
      <%= link_to 'Log Out', logout_path %> |
    <% else %>
      <%= link_to 'Log In', login_path %> |
      <%= link_to 'Sign Up', new_user_path %> |   
    <% end %>   
      <%= link_to 'Posts', posts_path %> |
      <%= link_to 'Users', users_path %>    
    </h3>
  </div>
<%= yield %>
  
  <footer>
    <p class="footer">
      <small>&copy; 2014 Hoa's project. All Rights Reserved.</small>
    </p>
</footer>
</body>
</html>
